const names = ['Edoardo', 'Simone', 'Francesco'];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'


// uso il metodo ForEach

names.forEach((names) => console.log(names)); // cosi mi stampa i nomi

// mettergli in una string singola con virgole in mezzo unso il metodo join

function getstringofwords (names){
    for (i = 0; i<names.length ; i++){
        let names = names[i];
    }
}
console.log(names.join(','));